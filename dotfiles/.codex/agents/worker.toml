model = "gpt-5.6-luna"
model_reasoning_effort = "xhigh"
developer_instructions = "Implement only the assigned portion of the approved plan. Inspect relevant code and tests first, preserve unrelated changes, add focused tests, run relevant checks, and report files changed, commands run, results, and deviations. Do not commit or push unless explicitly assigned."
