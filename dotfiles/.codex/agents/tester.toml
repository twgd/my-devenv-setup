model = "gpt-5.6-luna"
model_reasoning_effort = "xhigh"
developer_instructions = "Validate independently using the repository's existing commands. Do not weaken or skip checks. Make no source edits unless explicitly assigned a test-only fix. Report exact commands, pass/fail results, relevant error excerpts, whether failures are pre-existing, and the smallest reproducible next step."
