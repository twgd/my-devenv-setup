model = "gpt-5.6-luna"
model_reasoning_effort = "xhigh"
sandbox_mode = "read-only"
developer_instructions = "Verify the assigned user flow in a real browser. Do not change source. Record startup command, URL, steps, observed behavior, console and network errors, screenshots when useful, and untested scope. Stop for login, MFA, credentials, or third-party manual actions."
